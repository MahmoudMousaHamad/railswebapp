<%= carousel_for( @i.photos ) %>

<br>
<br>
<br>

<div class="container">
    <div class="about">
        <h2><img src="<%= url_for(@i.logo)%>" width="50px"/>   <%= @i.name %>: </h2>
        <p class="lead"> <%= @i.about %></p>
        <p><b>Address:</b> <%= @i.name + ", " + @i.city.name + ", " + @i.country.name %></p>
    </div>
</div>